"""detonate — a dynamic, sandboxed security scanner for MCP servers and AI tools.

Unlike static scanners that only READ a tool's manifest, detonate RUNS the
untrusted tool inside a disposable sandbox and reports what it actually does.
See ARCHITECTURE.md for the full picture.
"""

__version__ = "0.0.1"
